-- Returns all client mouse and keybinds.
return {
   keys    = require(... .. '.keys'),
   leaved  = require(... .. '.leaved'),
   mpd     = require(... .. '.mpd'),
   xf86    = require(... .. '.xf86'),
   mouse   = require(... .. '.mouse')
}
